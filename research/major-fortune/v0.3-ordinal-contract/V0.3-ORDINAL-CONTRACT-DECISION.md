# Major Fortune V0.3 Ordinal Contract Decision

## Decision

**`ORDINAL_CONTRACT_READY_FOR_ADAPTER`**

## Governance classification

| Field | Value |
| --- | --- |
| modelNature | `engineering-heuristic` |
| doctrineRelationship | `doctrine-informed-not-classical-reconstruction` |
| numericAuthority | `engineering-defined` |
| productionStatus | `research-only` |

V0.3 is an explainable engineering model informed by Tử Vi concepts. It is **not** claimed to reconstruct a classical numeric formula.

## Relationship to V0.2

V0.2 remains the historical doctrine-oriented research line and retains:

- `RESEARCH_INCOMPLETE`
- `SOURCE_GAPS_REMAIN`
- zero candidate-eligible scoring families
- zero eligible shape fragments

Those historical decisions are **not** revised by this contract.

## Readiness checklist

| Criterion | Status |
| --- | --- |
| Formula deterministic | yes |
| Four pillar budgets sum to 100 | yes |
| Every pillar level ordinal ∈ {-2…2} | yes |
| No per-rule rawDelta | yes |
| Exclusions enforced | yes |
| Dedupe policy explicit | yes |
| Coverage implemented | yes |
| Synthetic fixtures pass | yes |
| School gates fail closed | yes |
| Annual/monthly inputs rejected | yes |
| Production routing unchanged | yes (`unavailable` / `rebuilding`) |

## Formula

```text
pillarDelta = budget × level / 4
score = clamp(50 + Σ delta, 0, 100)  // precision 1
```

Budgets: `thien-thoi=30`, `dia-loi=25`, `nhan-hoa=20`, `tu-hoa-sat-tinh=25`.

## Evaluator API

```ts
evaluateMajorFortuneOrdinal(input: MajorFortuneOrdinalEvaluationInput): MajorFortuneOrdinalResult
```

Pure function: no file I/O, no env, no `ChartData`, no wall-clock year, deterministic, non-mutating, fail-closed on invalid contracts.

## Next step

Adapter handoff: map `ChartData` → normalized `MajorFortuneOrdinalEvidence` without changing this formula contract. See `prompts/next-step-handoff-prompt.md`.
