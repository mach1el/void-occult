# V0.9 Candidate Handoff Prompt

**Final readiness state from this foundation task: `RESEARCH_INCOMPLETE`.**
(See `V0.9-FOUNDATION-DECISION.md` for the full reasoning.) This prompt is
therefore a **research completion** task, not a V0.9 implementation task — do
not implement a new scoring engine or new production rules from this prompt.

---

## Context

Repository: `st-mich43l/apexvoid-occult`. Branch off latest `master` as
`research/ziwei-annual-axes-v0-9-research-completion`.

The prior task (`research/ziwei-annual-axes-v0-9-foundation`) produced a
complete, deterministic 100×12-chart audit of the Nam Phái Annual Axes V0.8
engine (`research/annual-axes/v0.9-foundation/audit/`) and a doctrine/policy
research pack (`research/annual-axes/v0.9-foundation/{sources,policy}/`). The
engineering side of that audit is solid and fully evidenced. The doctrine side
is not: the pack has **zero** sources of type `classical-text`,
`school-manual`, or `published-reference` (see
`sources/source-review-log.md`), so **zero** claims carry `status: "classical"`,
and one real contradiction (`CONTRA-AAV09-001`, Lưu Đào Hoa polarity) remains
logged but unadjudicated (`insufficient-evidence`).

## What this task must do

1. **Obtain at least one genuinely verified classical or school-manual Nam
   Phái source** with checkable locators (specific edition, page/chapter) —
   not general knowledge. Use `prompts/source-extraction-prompt.md`.
2. **Re-adjudicate** the following, using `prompts/claim-adjudication-prompt.md`:
   - `CONTRA-AAV09-001` (Lưu Đào Hoa romance polarity) — resolve or explicitly
     re-confirm `remain-disputed`/`school-specific` with real evidence.
   - The 12 stars in `policy/star-domain-policy.v0.9.json` →
     `unreferencedButEmittedStars` (Lưu Hồng Loan, Lưu Hỷ Thần, Lưu Kiếp Sát,
     Lưu Long Đức, Lưu Nguyệt Đức, Lưu Phúc Đức, Lưu Thiên Đức, Lưu Thiên Hỷ,
     Lưu Thiên Mã, Lưu Văn Khúc, Lưu Văn Xương, plus Đào Hoa above) — each
     needs an individually source-backed domain + polarity + point-class
     assignment before it can move past `engineering-hypothesis`.
   - The dignity topic (`policy/dignity-policy.v0.9.json`) and the Tiểu Hạn
     domain-asymmetry question (`CLM-AAV09-018`), both currently
     `RESEARCH_INCOMPLETE`.
3. **Do not** attempt to re-source the five confirmed-unsupported stars (Lưu
   Đại Hao, Lưu Tiểu Hao, Lưu Phục Binh, Lưu Tuần, Lưu Triệt) as a scoring
   question — they are `CALCULATION_CORE_BLOCKED` (verified zero corpus
   emissions; no producer exists). Any future work on them starts with
   Calculation Core, not doctrine research. See
   `policy/unsupported-star-policy.v0.9.json`.
4. Re-run `npm run research:annual-axes-v09:validate` after every registry
   edit and keep it green.
5. At the end, re-issue the four-state readiness classification
   (`READY_FOR_V0_9_CANDIDATE` / `RESEARCH_INCOMPLETE` /
   `V0_8_SHOULD_REMAIN_UNCHANGED` / `CALCULATION_CORE_BLOCKED`) in an updated
   `V0.9-FOUNDATION-DECISION.md`, honestly reflecting whatever sourcing was
   actually obtained — do not upgrade the verdict just because more text was
   written.

## One item that does NOT need new sourcing (separate, smaller task)

`audit/audit-findings.md` Finding 6 documents a verified, source-independent
engineering bug candidate: `scoreState` classification uses strict
`=== 0`/`!== 0` comparison on floating-point sums, which can misclassify a
true balanced-signal case as `"scored"` (confirmed: 107/7,200
domain-observations in the full corpus, root-caused to a `5.55e-17` epsilon).
This is a **Point Policy (category D)** fix, fully scoped, requiring zero
doctrinal sourcing:

- Change the zero-comparisons in
  `src/lib/ziwei/analysis/modules/annual-axes/nam-phai-v08/score-domain.ts`
  (`matchedStarCount === 0 && prominenceAdjustedRaw === 0`, etc.) to an
  epsilon-tolerant comparison (e.g. `Math.abs(x) < 1e-9`).
- This changes production V0.8 behavior (the `scoreState` label, not the
  numeric score) — it is out of scope for a "research-only" task and needs
  its own PR, its own regression fixture updates, and explicit sign-off,
  since the task that produced this research pack was expressly forbidden
  from touching V0.8 production code.
- If picked up, treat it as independent of the doctrine-sourcing work above —
  it can proceed immediately regardless of how the research completion above
  turns out.
