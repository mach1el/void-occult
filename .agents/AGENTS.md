
## Git Workflow Rules
- NEVER push directly to the `master` or `main` branch. Always create a feature or fix branch (e.g., `feat/...` or `fix/...`), commit your changes there, push the branch to origin, and let the user review and merge via Pull Request.
